<html lang="en">
    <%- include('./partials/head.ejs') %>
    <body>
        <%- include('./partials/nav.ejs') %>
        <div class="artists content">
            <form action="/artists" method="POST">
                <b>artists: </b></br> 
            <% if (theArtists.length > 0) { %>
                <% theArtists.forEach(artist => { %>
                    <b><%= artist.artist_name %></b>
                    <a href="/artists/<%=artist._id%>/edit?Name=<%=artist.name%>">edit</a>
                    <a href="/artists/<%=artist._id%>/delete?Name=<%=artist.artist_name%>">edit</a>
                    <a href="/artists/<%=artist._id%>/replace?Name=<%=artist.artist_name%>">edit</a>

                    </br>
                    <% }) %>
            <% } %>
            </br><label>Add Artist:</label></br>
            <label>Name:
                <input type="text" id="artistName" name="artist_name" value="<%= locals.Name %>" required>
            </label>
           
            </br>
            <button>POST</button>
        </form>
        </div>
    </body>
    <%- include('./partials/foot.ejs') %>
</html>